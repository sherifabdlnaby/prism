inputs:
    http_server:
        plugin: dummy_input
        config:
            filename: "input/test@{fileNum}.png"
            pipeline: "profile_pic_pipeline"
            timeout: 5000
            tick: 2000