processors:
    dummy_processor1:
        concurrency: 100
        plugin: vips
        config:
            operations:
                resize:
                    width: "@{width}"
                    height: "@{height}"
                    pad: background
                flip:
                    direction: horizontal