processors:
    dummy_processor1:
        concurrency: 100
        plugin: vips
        config:
            operations:
                resize:
                    width: "@{width}"
                    height: "@{height}"
                    pad: copy
#                blur:
#                    sigma: 1.1
    dummy_processor2:
        concurrency: 100
        plugin: vips
        config:
            operations:
                crop:
                    width:  300
                    height: 300
                    anchor: entropy
    dummy_processor3:
        concurrency: 100
        plugin: vips
        config:
            operations:
                flip:
                    direction: vertical