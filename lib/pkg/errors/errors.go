// Package errors define errors that are used throughout the codebase
package errors

import (
	"errors"
)

// Errors used throughout the codebase
var (
	ErrDummy = errors.New("dummy Error that has no meaning")
	/* More to be added */
)
